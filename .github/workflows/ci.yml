name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  general_checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Yarn
        uses: ./.github/workflows/actions/setup-yarn

      - name: Build
        run: yarn run build
